
require './app'
require 'mongoid'
require 'rake/testtask'

Rake::TestTask.new do |t|

  t.libs << "test"
  t.test_files = FileList['test/model_test.rb', 'test/service_test.rb']
  t.verbose = true

end

# calling rake seed to seed the data
task :seed do
  ruby 'seed/seed.rb'
end